pipeline {
    agent { label 'prodigy-commissary-dev' }

    stages {
        stage('Build') {
            steps {
                 sh '''
                    ls -al
                    cd JenkinsTesting
                    ls -al
                 '''
                 sh 'ls -al'
            }
        }
    }
}